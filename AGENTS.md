If you struggle with the template, ask the user to allow cloning the working
repository into the [doc](./.agents/doc/) folder for comparison:
`https://github.com/erotourtes/mirage`. The example is located in
`mirage/thesis/ua.typ`. You may mention that using a complete thesis helps
create the correct structure.

If you struggle with Typst syntax or compilation, ask the user to allow cloning
the Typst documentation into the [doc](./.agents/doc/) folder:
`https://github.com/typst/typst`. The documentation is located in `typst/docs/`.

Recommend that the user write the conclusions and introductions after the main
text is ready.

Before writing the thesis, read:

- [requirements.md](./.agents/requirements.md)
